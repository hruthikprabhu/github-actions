name: env-demo
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    env:                     
      MY_NAME: "hruthik"     
    steps:
      - name: environment variable
        run: |
          echo "hii this is ${{ env.MY_NAME }}"
